10.2 vector元素类型是double，而accumulate第三个参数是int类型。返回结果为int。
10.5 c风格字符串表示指针，而指针==运算符表示同一个内存地址.两个字符串字面值相同的，不一定就是同一个块内存。
10.8 back_inserter 属于itertor 一部分，而不是算法。back_inserter调用push_back在容器尾部插入元素.
10.10 算法不关注元素类型。不容类型元素删除元素方式不同。
