7.8 read ?????βζ???Ϊ??ͨ????,??Ϊ??Ҫ?ı???��;print?????βζ???const????,??Ϊ??��ֵ????Ҫ?ı䣬ֻ?��?
7.10 if(read(read(cin,data1),data2))��????ȡ2??

7.25 Screen??????��Ĭ?ϵĿ????͸?ֵ?汾.??Ϊscreen??Ա??��Ϊ????????,???ж?ӦĬ?Ͽ????͸?ֵ???캯??.
7.16 访问说明符出现次数没有限制。类的接口应该定义在public之后。类的实现细节，应该定义在private之后。
7.17 class 和 struct定义类的唯一区别，是默认访问权限：class默认访问权限是private，struct默认访问权限是public.
7.18 封装屏蔽类的实现细节。
7.20 友元提供类外函数或是类，访问类中私有成员方式。友元可以使提供访问类中私有成员方式。同时，也破坏类的封装。
7.34 pos定义private变量提示未定义类型。
7.38 Sales_data(std::istream& out  = std::cin){read(out,*this);}
7.39 不合法。一个类只能有一个默认构造函数.会导致函数匹配二义性。
7.44 不合法。vector 构造一个10个大小的默认对象。NoDefault没有默认构造函数。
7.45 合法。vector元素类型为C。C调用NoDefault的普通构造函数。
7.46 (a)不对。如果类没有定义任何构造函数，编译器会合成构造函数。
    (b) 不对。或是所有的形参，都有默认值。
    (c) 不对。避免获取脏数据，应该给成员提供初始值。
    (d) 不对。当类成员有类类型时候，类类型没有默认构造函数。类不能完成初始化。
7.47 接受单一string的Sales_data构造函数声明为explicit（即抑制隐式类型转换）;构造初始化只能使用直接初始化
不便：只能使用构造函数接受一个参数，实现构造。
7.48 都可以。
7.49 (a) string类型的s，会调用Sales_data(string)构造函数，构造临时Sales_data对象。
    (b) Sales_data& 类型不能转换成Sales_data类型。
    (c)形参列表后的const与combine语义冲突。const禁止修改this变量成员。
7.50 explicit只适用单一参数的构造函数。
7.51 string 需要将指针表示的字符串，构造出一个string类型。需要类型转换。
    vector接受一个int的，如果类型转换无意义。
7.54 set开头函数,会修改成员值.constexpr 限定的函数,是一种隐式const类型.(c++11)。
7.55 Data类不是字面值常量类.字面值常量类:1,成员都必须是字面值类型.字面值类型包括算数类型,引用,指针及某些类类型.2.类必须至少含有一个constexpr类型函数.(不满足);
3.如果类成员含有类内初始值,则初始值必须是constexpr表达式或类成员为类类型,必须用成员自己的constexpr构造函数初始化。4.类的析构函数必须使用默认定义,销毁对象.
string不是字面值类型。
7.56 静态成员 不属于对象只属于类。
     同一个类，只存储一份静态成员。
     静态成员可以是不完全类型；静态成员可以作为默认参数，普通成员不可以。
7.58 ex7_58.h ex7_58.cpp 
